# Single-package project — no workspaces.
# This file tells pnpm this is the workspace root,
# preventing it from walking up to a parent directory's lockfile.
packages:
  - "."
allowBuilds:
  '@prisma/client': true
  '@prisma/engines': true
  esbuild: true
  prisma: true
  sharp: true
  unrs-resolver: true
